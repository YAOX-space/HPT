# Legacy Materials

This folder contains pre-HPT2.0 materials moved out of the active mainline.

- `hpt_frt/`: previous FRT controller/package implementation retained for traceability.
- `tests_hpt_frt/`: tests that exercise the legacy `hpt_frt` package.

Current HPT2.0 work should use the root-level `sac/`, `simulink/`, `experts/`,
`results/`, and `tests/` folders.
