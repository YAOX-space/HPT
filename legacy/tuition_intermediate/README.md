# Archived Tuition Intermediate Files

This folder contains pre-final tutorial snapshots and early one-off test
outputs moved out of `tuition/`.

The active tutorial entry point is `tuition/hpt_topology1_tutorial.slx`.
